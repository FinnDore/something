
/**
 * Describes a state of an event. the event can used to describe rest calls for example
 */
export enum EventState {
    INIT = "INIT",
    LOADING = "LOADING",
    OK = "OK",
    ERROR = "ERROR"
}